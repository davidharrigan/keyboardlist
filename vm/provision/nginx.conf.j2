#{{ ansible_managed }}
server {
    listen 80;
    server_name {{ server_name }};
    location / {
        proxy_set_header   X-Real-IP $remote_addr;
        proxy_set_header   Host      $http_host;
    }
}
